import React, { PureComponent } from 'react';

export default class Hero extends PureComponent {
    state = {
    }

    static defaultProps = {
    }

    render() {
        return (
            <div className="Hero">
                Hero
            </div>
        );
    }
}
